name: JARVIS Integrated Evolution
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  evolution_cycle:
    name: "🧬 Ciclo de Evolução"
    if: github.event_name != 'pull_request' && !contains(github.event.head_commit.message, '[Auto-Evolution]')
    runs-on: ubuntu-latest
    outputs:
      action: ${{ steps.flow.outputs.action }}
      pr_number: ${{ steps.create_pr.outputs.pr_number }}
      branch: ${{ steps.create_pr.outputs.branch }}
      mission_desc: ${{ steps.flow.outputs.mission_desc }}
    steps:
      - name: 🛰️ Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.JARVIS_PAT || secrets.GITHUB_TOKEN }}

      - name: 📦 Setup UV
        uses: astral-sh/setup-uv@v5

      - name: 🧠 Orquestrador e Mutação
        id: flow
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GH_TOKEN: ${{ secrets.JARVIS_PAT || secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}
        run: |
          uv venv --python 3.11
          uv pip install sqlmodel pydantic requests groq python-dotenv
          PY_VENV=".venv/bin/python"
          $PY_VENV - << 'PYCODE'
          import os, sys
          from app.application.services.auto_evolutionV2 import AutoEvolutionServiceV2
          try:
              auto = AutoEvolutionServiceV2()
              next_m = auto.find_next_mission()
              with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                  if next_m:
                      with open('mission_context.txt', 'w', encoding='utf-8') as mc:
                          mc.write(auto.get_roadmap_context(next_m))
                      f.write(f"action=evolve\nmission_desc={next_m['id']}: {next_m['title']}\n")
                  else: f.write("action=idle\n")
          except Exception as e: sys.exit(1)
          PYCODE

      - name: 🤖 Mutação e Abertura de PR
        id: create_pr
        if: steps.flow.outputs.action == 'evolve'
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GH_TOKEN: ${{ secrets.JARVIS_PAT || secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}
        run: |
          git config --global user.name "Jarvis-Bot"
          git config --global user.email "jarvis@bot.com"
          PY_VENV=".venv/bin/python"
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          
          # 1. Cristalização
          $PY_VENV app/application/services/crystallization/crystallizer_engine.py
          
          # 2. Setup e Push Imediato (Garante checkout na Homeostase)
          BRANCH_NAME="auto-evolution/mission-$(date +%s)"
          git checkout -b "$BRANCH_NAME"
          git commit --allow-empty -m "🧬 [Auto-Evolution] Iniciando Missão: ${{ steps.flow.outputs.mission_desc }}"
          git push origin "$BRANCH_NAME"
          echo "branch=$BRANCH_NAME" >> $GITHUB_OUTPUT
          
          # 3. Mutação (DNA Injection)
          if ! $PY_VENV scripts/evolution_mutator.py \
            --strategy "minimal_change" \
            --intent "auto-evolution-mission" \
            --impact "functional-improvement" \
            --roadmap-context "$(cat mission_context.txt)"; then
              echo "❌ Falha no mutator, a Homeostase assumirá o controle."
              exit 1
          fi

          git add -A
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "[Auto-Evolution] DNA Mutated: ${{ steps.flow.outputs.mission_desc }}"
            git push origin "$BRANCH_NAME"
            PR_URL=$(gh pr create --title "🧬 Evolução: ${{ steps.flow.outputs.mission_desc }}" \
                         --body "Auto-Cura em andamento." --base main --head "$BRANCH_NAME")
            echo "pr_number=$(echo $PR_URL | awk -F'/' '{print $6}')" >> $GITHUB_OUTPUT
          fi

  homeostasis:
    name: "🧪 Homeostase e Auto-Cura"
    if: always() && !cancelled()
    needs: evolution_cycle
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ needs.evolution_cycle.outputs.branch || github.event.pull_request.head.ref || github.ref }}
          token: ${{ secrets.JARVIS_PAT || secrets.GITHUB_TOKEN }}

      - name: 📦 Setup UV
        uses: astral-sh/setup-uv@v5

      - name: 🔧 Cura e Consolidação
        id: healing
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GH_TOKEN: ${{ secrets.JARVIS_PAT || secrets.GITHUB_TOKEN }}
          PYTHONPATH: ${{ github.workspace }}
        run: |
          uv venv --python 3.11
          # Adicionado pytest-cov para satisfazer o pytest.ini
          uv pip install pytest pytest-json-report pytest-cov pydantic sqlmodel groq requests python-dotenv
          PY_VENV=".venv/bin/python"
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          
          SUCCESS=false
          for i in {1..2}; do
            # Ignoramos flags de coverage do ini para evitar erros se necessário, mas o plugin já foi instalado
            .venv/bin/pytest -p no:cov --json-report --json-report-file=report.json tests/ || true
            
            if [ -f report.json ] && [ "$(jq '.summary.failed // 0' report.json)" == "0" ]; then
              SUCCESS=true && break
            fi
            
            # Se não há report.json (falha grave de execução), usamos log genérico
            if [ ! -f report.json ]; then
               echo "🚨 Falha estrutural detectada. Tentando cura cega..."
            fi
            
            $PY_VENV scripts/self_healing_mutator.py --report "report.json"
            git config --global user.name "Jarvis-Bot"
            git config --global user.email "jarvis@bot.com"
            git add -A
            if [ -n "$(git status --porcelain)" ]; then
              git commit -m "🤖 [Auto-Cura] Estabilização #$i"
              git push origin HEAD
            fi
          done
          echo "final_status=$SUCCESS" >> $GITHUB_OUTPUT

      - name: ✅ Auto-Merge
        if: steps.healing.outputs.final_status == 'true' && (github.event.pull_request.number || needs.evolution_cycle.outputs.pr_number)
        env:
          GH_TOKEN: ${{ secrets.JARVIS_PAT || secrets.GITHUB_TOKEN }}
        run: |
          PR_NUM="${{ github.event.pull_request.number || needs.evolution_cycle.outputs.pr_number }}"
          gh pr merge $PR_NUM --auto --merge
