name: JARVIS Integrated Evolution
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  evolution_cycle:
    name: "🧬 Ciclo Unificado JARVIS"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install Deps
        run: pip install sqlmodel pydantic requests

      - name: 🧠 Orquestrador e Sincronizador
        id: flow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          export PYTHONPATH="${PYTHONPATH:+$PYTHONPATH:}$(pwd)"
          python - << 'PYCODE'
          import os, sys, re
          from app.application.services.auto_evolutionV2 import AutoEvolutionServiceV2
          try:
              auto = AutoEvolutionServiceV2()
              last_msg = os.popen("git log -1 --pretty=%B").read()
              is_sync = "[Auto-Evolution] Inventory Synchronized" in last_msg
              
              if is_sync:
                  out = "action=idle\n"
              else:
                  next_m = auto.find_next_mission()
                  if next_m:
                      cap_id = next_m['id']
                      with open('mission_context.txt', 'w', encoding='utf-8') as f:
                          f.write(auto.get_roadmap_context(next_m))
                      out = f"action=evolve\nmission_desc={cap_id}: {next_m['title']}\n"
                  else:
                      out = "action=idle\n"
              with open(os.environ['GITHUB_OUTPUT'], 'a') as f: f.write(out)
          except Exception as e:
              print(f"❌ Erro: {e}"); sys.exit(1)
          PYCODE

      - name: 🤖 Cristalização e Mutação
        if: steps.flow.outputs.action == 'evolve'
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_BODY: ${{ steps.flow.outputs.mission_desc }}
        run: |
          git config --global user.name "Jarvis-AutoEvolution"
          git config --global user.email "jarvis@bot.com"
          
          # 1. Cristaliza primeiro (Cria a casca)
          export PYTHONPATH="${PYTHONPATH:+$PYTHONPATH:}$(pwd)"
          python app/application/services/crystallization/crystallizer_engine.py
          
          # 2. Prepara Branch de Evolução
          BRANCH_NAME="auto-evolution/mission-$(date +%s)"
          git checkout -b "$BRANCH_NAME"
          
          # 3. Muta (Preenche a lógica)
          python scripts/evolution_mutator.py --strategy "minimal_change" --intent "auto-evolution-mission" --roadmap-context "$(cat mission_context.txt)"
          
          # 4. Commit Unificado (Código + Estrutura + Master Crystal)
          git add -A
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "[Auto-Evolution] DNA Mutated & Crystallized: $ISSUE_BODY"
            git push origin "$BRANCH_NAME"
            gh pr create --title "🧬 Evolução: $ISSUE_BODY" --body "Automated evolution and crystallization cycle." --base main --head "$BRANCH_NAME"
          fi
