name: "🧬 JARVIS: Homeostase e Auto-Cura V2"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  homeostasis:
    name: "🧪 Vistoria e Auto-Cura"
    # Aspas simples garantem que o GitHub não se perca com os parênteses e exclamações
    if: '${{ !contains(github.event.head_commit.message, "[Auto-Cura]") && !contains(github.event.head_commit.message, "[Auto-Evolution]") }}'
    runs-on: ubuntu-latest

    steps:
      - name: 🛰️ Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup UV
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: 🔧 Preparar Ambiente
        run: |
          uv venv --python 3.13
          uv pip install pytest pytest-json-report requests sqlmodel pydantic groq
          echo "PYTHONPATH=$PYTHONPATH:$(pwd)" >> $GITHUB_ENV

      - name: 🔄 Ciclo de Cura e Decisão
        id: healing_process
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          ATTEMPT=1
          SUCCESS=false
          FINAL_MSG="Iniciado"

          while [ $ATTEMPT -le 2 ]; do
            echo "🧪 Tentativa $ATTEMPT..."
            
            # Se o teste passar, sucesso absoluto
            if uv run pytest --json-report --json-report-file=report.json tests/; then
              echo "✅ Sistema saudável."
              SUCCESS=true
              FINAL_MSG="Sucesso"
              break
            fi
            
            # Se falhou, prepara log curto para o Mecânico
            ERR_DATA=$(cat report.json | jq -c '.tests[] | select(.outcome == "failed") | {id: .nodeid, msg: .call.crash.message}' | head -c 500)
            
            # Consulta ao Mecânico Revisionador
            RISK_OUT=$(uv run python scripts/metabolism_analyzer.py --intent "self-healing" --instruction "Reparo" --context "Log: $ERR_DATA")
            
            if echo "$RISK_OUT" | grep -q '"requires_human": true'; then
              FINAL_MSG=$(echo $RISK_OUT | jq -r '.reason')
              SUCCESS=false
              break
            fi

            # Tentativa de Cura via Mutator
            uv run python scripts/self_healing_mutator.py --report "report.json"
            
            git config --global user.name "Jarvis-AutoEvolution"
            git config --global user.email "jarvis@bot.com"
            git add -A
            git commit -m "🤖 [Auto-Cura] Tentativa #$ATTEMPT" || echo "Sem mudanças"
            
            ATTEMPT=$((ATTEMPT+1))
          done

          echo "final_status=$SUCCESS" >> $GITHUB_OUTPUT
          echo "FINAL_REASON=$FINAL_MSG" >> $GITHUB_ENV

      - name: ✅ Auto-Merge
        if: steps.healing_process.outputs.final_status == 'true' && github.event.pull_request.number != null
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ github.event.pull_request.user.login }}" = "TheDrack" ]; then
            gh pr merge ${{ github.event.pull_request.number }} --auto --merge
          fi

      - name: 🚨 Escalação
        if: steps.healing_process.outputs.final_status == 'false' && env.FINAL_REASON != 'Sucesso'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create --title "🚨 Homeostase V2: @TheDrack" --body "Falha persistente. Motivo: ${{ env.FINAL_REASON }}"
          exit 1
