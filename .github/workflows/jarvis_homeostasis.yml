name: "🧬 JARVIS: Homeostase e Auto-Cura"

on:
  push:
    branches: [ main ]
  pull_request_target:
    branches: [ main ]
    types: [opened, synchronize, reopened]

# Evita execuções simultâneas para a mesma PR/Branch
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: write

jobs:
  homeostasis:
    name: "🧪 Vistoria e Auto-Cura"
    # Lógica para não rodar duplicado: prioriza pull_request_target
    if: |
      (github.event_name == 'pull_request_target') || 
      (github.event_name == 'push' && !contains(github.event.head_commit.message, '🤖 [Auto-Cura]'))
    runs-on: ubuntu-latest

    steps:
      - name: 🛰️ Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          fetch-depth: 0

      - name: 📦 Setup Environment (UV)
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: 🔧 Instalar Dependências
        run: |
          uv venv --python 3.13
          uv pip install pytest pytest-json-report pytest-cov requests sqlmodel pydantic groq python-jose[cryptography] bcrypt
          echo "PYTHONPATH=$PYTHONPATH:$(pwd)" >> $GITHUB_ENV

      - name: 🧪 Executar Pytest
        id: run_tests
        continue-on-error: true
        run: |
          uv run pytest --json-report --json-report-file=report_1.json tests/

      - name: 🔬 Análise de Risco (Mecânico)
        id: risk_check
        run: |
          uv run python scripts/metabolism_analyzer.py --intent "auto-evolution" --instruction "Validar autonomia" --repo-path "." > analysis_result.txt
          if grep -q '"requires_human": true' analysis_result.txt; then
            echo "HIGH_RISK=true" >> $GITHUB_ENV
          else
            echo "HIGH_RISK=false" >> $GITHUB_ENV
          fi

      - name: ✅ Auto-Merge
        if: |
          steps.run_tests.outcome == 'success' && 
          github.event.pull_request.number != null && 
          (github.event.pull_request.user.login == 'TheDrack' || env.HIGH_RISK == 'false')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh pr merge "${{ github.event.pull_request.number }}" --auto --merge --repo "${{ github.repository }}"

      - name: 🔧 Ciclo de Cura
        if: steps.run_tests.outcome == 'failure'
        id: heal_step_1
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GH_TOKEN: ${{ github.token }}
          # Mapeamento explícito para o Bash ler as variáveis do GitHub
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
          PR_USER: ${{ github.event.pull_request.user.login }}
          ACTOR: ${{ github.actor }}
        run: |
          uv run python scripts/self_healing_mutator.py --report "report_1.json"
          
          # Escolha da branch e usuário com fallbacks seguros
          TARGET_BRANCH="${PR_HEAD_REF:-main}"
          CURRENT_USER="${PR_USER:-$ACTOR}"
          
          if [ "${{ env.HIGH_RISK }}" == "true" ] && [ "$CURRENT_USER" != "TheDrack" ]; then
            echo "stop_healing=true" >> $GITHUB_ENV
            echo "🛑 Risco detectado para usuário $CURRENT_USER. Bloqueando cura automática."
          else
            git config --global user.name "Jarvis-AutoCura"
            git config --global user.email "jarvis@bot.com"
            git add .
            # Só comita se houver mudanças reais
            if git diff --staged --quiet; then
              echo "Nada para corrigir."
            else
              git commit -m "🤖 [Auto-Cura] Evolução validada"
              # Push agora possui uma variável garantida
              git push origin HEAD:"$TARGET_BRANCH"
              uv run pytest --json-report --json-report-file=report_2.json tests/ || echo "still_failing=true" >> $GITHUB_ENV
            fi
          fi

      - name: 🚨 Escalonar
        if: always() && (env.final_failure == 'true' || env.stop_healing == 'true')
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number || github.event.number }}
        run: |
          if [ -n "$PR_NUMBER" ] && [ "$PR_NUMBER" != "null" ]; then
            gh pr edit "$PR_NUMBER" --add-label "commander-review" --repo "${{ github.repository }}"
            gh pr comment "$PR_NUMBER" --repo "${{ github.repository }}" --body "## 🚨 INTERVENÇÃO NECESSÁRIA
            A auto-cura foi interrompida para proteger o DNA.
            **Motivo:** ${{ env.stop_healing == 'true' && 'Risco Arquitetural' || 'Falha persistente' }}"
          fi
