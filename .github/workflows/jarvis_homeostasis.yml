name: "🧬 JARVIS: Homeostase e Auto-Cura"

on:
  push:
    branches: [ main ]
  pull_request_target:
    branches: [ main ]
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: write

jobs:
  homeostasis:
    name: "🧪 Vistoria e Auto-Cura"
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛰️ Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          fetch-depth: 0

      - name: 📦 Setup Environment (UV)
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: 🔧 Instalar Dependências
        run: |
          uv venv --python 3.13
          uv pip install pytest pytest-json-report pytest-cov requests sqlmodel pydantic groq python-jose[cryptography] bcrypt google-api-python-client google-auth-httplib2 google-auth-oauthlib
          echo "PYTHONPATH=$PYTHONPATH:$(pwd)" >> $GITHUB_ENV

      - name: 🧪 Executar Pytest
        id: run_tests
        continue-on-error: true
        run: |
          uv run pytest --json-report --json-report-file=report_1.json tests/

      # --- SYNC DRIVE (Caminho Corrigido) ---
      - name: ☁️ Sync Google Drive
        if: always()
        env:
          G_JSON: ${{ secrets.G_JSON }}
          DRIVE_FOLDER_ID: ${{ secrets.DRIVE_FOLDER_ID }}
        run: |
          # Ajustado para o caminho correto da arquitetura JARVIS
          uv run python app/adapters/infrastructure/drive_sync.py || echo "Aviso: drive_sync não encontrado ou falhou"

      - name: ✅ Auto-Merge
        if: steps.run_tests.outcome == 'success' && github.event.pull_request.number != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh pr merge "${{ github.event.pull_request.number }}" --auto --merge --repo "${{ github.repository }}"

      - name: 🔧 Ciclo de Cura 1
        if: steps.run_tests.outcome == 'failure'
        id: heal_step_1
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GH_TOKEN: ${{ github.token }}
        run: |
          uv run python scripts/self_healing_mutator.py --report "report_1.json"
          uv run python scripts/metabolism_analyzer.py --intent "auto-fix" --instruction "Reparar falhas" --repo-path "." > analysis_1.txt
          
          if grep -q '"requires_human": true' analysis_1.txt; then
            echo "stop_healing=true" >> $GITHUB_ENV
          else
            git config --global user.name "Jarvis-AutoCura"
            git config --global user.email "jarvis@bot.com"
            git add .
            git commit -m "🤖 [Auto-Cura] Aplicando correção"
            # Proteção para push em PR ou Main
            TARGET_REF=${{ github.event.pull_request.head.ref || 'main' }}
            git push origin HEAD:$TARGET_REF
            uv run pytest --json-report --json-report-file=report_2.json tests/ || echo "still_failing=true" >> $GITHUB_ENV
          fi

      - name: 🚨 Escalonar ao Comandante
        if: always() && (env.final_failure == 'true' || env.stop_healing == 'true')
        env:
          GH_TOKEN: ${{ github.token }}
          # Fallback para evitar erro de string vazia
          PR_NUMBER: ${{ github.event.pull_request.number || '0' }}
        run: |
          if [ "$PR_NUMBER" != "0" ]; then
            gh pr edit "$PR_NUMBER" --add-label "commander-review" --repo "${{ github.repository }}"
            gh pr comment "$PR_NUMBER" --repo "${{ github.repository }}" --body "## 🚨 INTERVENÇÃO NECESSÁRIA
            O metabolismo atingiu o limite de segurança. Revisão manual requerida."
          else
            echo "Detectada falha na branch main. Por favor, verifique os logs."
          fi
