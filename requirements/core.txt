# Core dependencies - cloud-ready, no hardware requirements
# These dependencies can run in headless Linux environment

# Configuration management
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Security and Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database ORM
sqlmodel>=0.0.14
